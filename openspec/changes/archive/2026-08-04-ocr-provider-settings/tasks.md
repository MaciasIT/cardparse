# Tareas T16 — Configuración visible de proveedor OCR en Ajustes

- [x] 1. Cargar `ProviderConfig` actual desde `loadProviderConfig()` en `SettingsScreen`.
- [x] 2. Añadir estado local editable para endpoint, modelo, API key enmascarada y estado activo.
- [x] 3. Mostrar estado del proveedor con feedback visible, incluyendo última guardada legible.
- [x] 4. Guardar cambios con `saveProviderConfig()` y validar campos obligatorios.
- [x] 5. Añadir toggle activar/desactivar proveedor desde Ajustes sin eliminar config.
- [x] 6. Añadir tests unitarios con mock de `saveProviderConfig()` / `loadProviderConfig()`.
- [x] 7. Ejecutar `npm run typecheck && npm run test`.
- [x] 8. Commit con mensaje `feat: T16 — configuración visible de proveedor OCR en Ajustes`.
